{% extends 'base.html.twig' %}

{% block title %}Book Subscribe{% endblock %}

{% block body %}


    <div class="example-wrapper">
    </div>

    <script>
        const eventSource = new EventSource("{{ mercure(topic)|escape('js') }}");
        eventSource.onmessage = event => {
            console.log(JSON.parse(event.data));
        }
    </script>

{% endblock %}
